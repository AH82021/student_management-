# Class Relationship Diagram

## 📐 Project Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    STUDENT MANAGEMENT SYSTEM                     │
│                         (Package Structure)                      │
└─────────────────────────────────────────────────────────────────┘

Package: com.studentmgmt


┌─────────────────────────────────────────────────────────────────┐
│                         INHERITANCE TREE                         │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │     Person      │ ◄─── ABSTRACT CLASS
                    │   (Abstract)    │      - Cannot be instantiated
                    │─────────────────│      - Defines common behavior
                    │ - name          │
                    │ - age           │
                    │ - id            │
                    │─────────────────│
                    │ + getName()     │
                    │ + setName()     │
                    │ + displayInfo() │ ◄─── Abstract method
                    │ + getRole()     │ ◄─── Abstract method
                    └────────┬────────┘
                             │
                     extends │ (IS-A relationship)
                             │
            ┌────────────────┴────────────────┐
            │                                 │
            ▼                                 ▼
    ┌───────────────┐                ┌───────────────┐
    │    Student    │                │    Teacher    │
    │───────────────│                │───────────────│
    │ - grade       │                │ - subject     │
    │ - major       │                │ - experience  │
    │───────────────│                │───────────────│
    │ + getGrade()  │                │ + getSubject()│
    │ + setGrade()  │                │ + isSenior()  │
    │ @Override     │                │ @Override     │
    │ displayInfo() │ ◄── Different  │ displayInfo() │ ◄── Implementation
    │ @Override     │     from       │ @Override     │
    │ getRole()     │     Teacher    │ getRole()     │
    └───────────────┘                └───────────────┘
         ▲                                  ▲
         │                                  │
         └──── POLYMORPHISM ────────────────┘
         Same method names, different behavior!


┌─────────────────────────────────────────────────────────────────┐
│                      INTERFACE IMPLEMENTATION                    │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────────┐
    │   «interface»    │ ◄─── INTERFACE
    │   Searchable     │      - Contract/Agreement
    │──────────────────│      - All methods abstract
    │ + searchById()   │      - No implementation
    │ + searchByName() │
    └────────┬─────────┘
             │
             │ implements (Contract fulfillment)
             │
             ▼
    ┌─────────────────────┐
    │  StudentManager     │ ◄─── MUST implement all
    │─────────────────────│      interface methods
    │ - students[]        │ ◄─── Array of Students
    │ - currentSize       │
    │─────────────────────│
    │ @Override           │
    │ + searchById()      │ ◄─── Implementation
    │ @Override           │
    │ + searchByName()    │ ◄─── Implementation
    │ + addStudent()      │
    │ + displayAll()      │
    └─────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                      STANDALONE CLASSES                          │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │      Course         │ ◄─── ENCAPSULATION Example
    │─────────────────────│      - All fields private
    │ - courseCode        │      - Public getters/setters
    │ - courseName        │      - Data validation
    │ - capacity          │
    │ - enrolledCount     │
    │─────────────────────│
    │ + getCourseCode()   │
    │ + setCourseCode()   │
    │ + enrollStudent()   │
    │ + isFull()          │
    └─────────────────────┘


    ┌──────────────────────────────────┐
    │  StudentManagementSystem         │ ◄─── MAIN CLASS
    │──────────────────────────────────│      - Entry point
    │ + main(String[] args)            │      - Menu system
    │──────────────────────────────────│      - Scanner usage
    │ - displayMenu()                  │
    │ - addNewStudent()                │
    │ - searchById()                   │
    │ - searchByName()                 │
    │ - addSampleData()                │
    └──────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                        CONCEPT MAPPING                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┬─────────────────────────────────────────┐
│   OOP CONCEPT        │   WHERE DEMONSTRATED                    │
├──────────────────────┼─────────────────────────────────────────┤
│ Abstraction          │ - Person (abstract class)               │
│                      │ - Searchable (interface)                │
├──────────────────────┼─────────────────────────────────────────┤
│ Inheritance          │ - Student extends Person                │
│                      │ - Teacher extends Person                │
├──────────────────────┼─────────────────────────────────────────┤
│ Polymorphism         │ - Student.displayInfo() vs              │
│                      │   Teacher.displayInfo()                 │
│                      │ - Person p = new Student();             │
├──────────────────────┼─────────────────────────────────────────┤
│ Encapsulation        │ - All classes (private variables)       │
│                      │ - Course (strong encapsulation)         │
└──────────────────────┴─────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                        USAGE FLOW                                │
└─────────────────────────────────────────────────────────────────┘

   User runs program
          │
          ▼
   ┌──────────────┐
   │     main()   │ ◄─── Creates Scanner & StudentManager
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Menu Loop   │ ◄─── Display menu, get choice
   └──────┬───────┘
          │
          ├─────► Add Student ────► Create Student object
          │                         Store in StudentManager
          │
          ├─────► Display All ────► StudentManager.displayAll()
          │                         Loop through array
          │
          ├─────► Search by ID ──► StudentManager.searchById()
          │                         Searchable interface method
          │
          ├─────► Search Name ───► StudentManager.searchByName()
          │                         Searchable interface method
          │
          └─────► Exit ──────────► scanner.close()


┌─────────────────────────────────────────────────────────────────┐
│                      DATA STRUCTURE                              │
└─────────────────────────────────────────────────────────────────┘

StudentManager contains:

    students[0] ───► Student { name="Alice", grade=3.8, major="CS" }
    students[1] ───► Student { name="Bob", grade=3.5, major="Math" }
    students[2] ───► Student { name="Charlie", grade=3.9, major="Physics" }
    students[3] ───► null
    students[4] ───► null
    ...
    students[99] ──► null

    currentSize = 3  (tracks how many students are actually stored)


┌─────────────────────────────────────────────────────────────────┐
│                   POLYMORPHISM IN ACTION                         │
└─────────────────────────────────────────────────────────────────┘

// Person reference can hold Student or Teacher objects!

Person[] persons = new Person[4];

persons[0] = new Student("Alice", ...);   ◄─── Person reference
persons[1] = new Teacher("Dr. Smith", ...);    points to Student
persons[2] = new Student("Bob", ...);          object
persons[3] = new Teacher("Prof. Lee", ...);

// When calling methods:
for (Person p : persons) {
    p.displayInfo();  ◄─── Calls Student version for students
                           Calls Teacher version for teachers
                           RUNTIME POLYMORPHISM!
}


┌─────────────────────────────────────────────────────────────────┐
│                      KEY RELATIONSHIPS                           │
└─────────────────────────────────────────────────────────────────┘

Student    IS-A   Person          (Inheritance)
Teacher    IS-A   Person          (Inheritance)
StudentManager IMPLEMENTS Searchable  (Interface)
StudentManager HAS-A Student[]    (Composition/Aggregation)
StudentManagementSystem USES StudentManager  (Dependency)
StudentManagementSystem USES Scanner         (Dependency)


┌─────────────────────────────────────────────────────────────────┐
│                    PROJECT FILES OVERVIEW                        │
└─────────────────────────────────────────────────────────────────┘

student-management-system/
│
├── pom.xml                           # Maven configuration
│
├── README.md                         # Complete project guide
├── PROJECT_SUMMARY.md                # Implementation summary
├── QUICK_REFERENCE.md                # Java syntax quick reference
├── CLASS_DIAGRAM.md                  # This file!
│
└── src/main/java/com/studentmgmt/
    │
    ├── Person.java                   # Abstract class (218 lines)
    ├── Student.java                  # Concrete class (190 lines)
    ├── Teacher.java                  # Concrete class (133 lines)
    ├── Searchable.java               # Interface (65 lines)
    ├── Course.java                   # Encapsulation (168 lines)
    ├── StudentManager.java           # Manager class (178 lines)
    └── StudentManagementSystem.java  # Main class (248 lines)

                    Total: ~1,200 lines of code
                    ~1,000 lines of educational comments!


┌─────────────────────────────────────────────────────────────────┐
│                         LEARNING PATH                            │
└─────────────────────────────────────────────────────────────────┘

    1. Person.java          ◄─── Start here (Abstraction)
           ↓
    2. Student.java         ◄─── Then this (Inheritance)
           ↓
    3. Teacher.java         ◄─── Similar to Student
           ↓
    4. Course.java          ◄─── Practice Encapsulation
           ↓
    5. Searchable.java      ◄─── Understand Interfaces
           ↓
    6. StudentManager.java  ◄─── Arrays & Implementation
           ↓
    7. StudentManagementSystem.java  ◄─── Tie it all together!
           ↓
    8. Test & Run! 🎉


┌─────────────────────────────────────────────────────────────────┐
│                    CONCEPTS BY COMPLEXITY                        │
└─────────────────────────────────────────────────────────────────┘

BASIC (Start here):
  ✓ Classes and Objects
  ✓ Instance variables
  ✓ Constructors
  ✓ this keyword
  ✓ Getters and Setters

INTERMEDIATE:
  ✓ Inheritance
  ✓ super keyword
  ✓ Method Overriding
  ✓ Arrays
  ✓ Scanner

ADVANCED:
  ✓ Abstract classes
  ✓ Interfaces
  ✓ Polymorphism
  ✓ Runtime polymorphism


┌─────────────────────────────────────────────────────────────────┐
│                      SUCCESS CRITERIA                            │
└─────────────────────────────────────────────────────────────────┘

Your implementation is complete when:
  ☐ All classes compile without errors
  ☐ You can create Student and Teacher objects
  ☐ You can add students to StudentManager
  ☐ Search by ID works
  ☐ Search by name works
  ☐ Display all students works
  ☐ Menu system works
  ☐ Scanner reads input correctly
  ☐ You understand each concept used
  ☐ You can explain the code to someone else


🎓 Good luck with your implementation!
```
